package java8;

/**
 * Stream is not a data structure,
 * it takes input from collections, array
 *
 *
 * Streams don't change the original data structure
 * They provide result as per pipelined methods
 * Streams only provide result as per pipelined methods
 *
 * Each intermediate operation is lazily executed and returns
 * a stream as a result, hence various immediate operations
 * can be pipelined. Terminal operations mark the end of
 * the stream and return the result
 */
public class JavaStreamExample {

    public void stream() {


    }
}
